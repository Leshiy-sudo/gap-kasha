package com.gapkassa.ui.theme

/**
 * Feature flag to toggle the Clean Fintech redesign on/off.
 * Set to false to fall back to the legacy UI without removing new code.
 */
object UiConfig {
    const val useCleanFintechRedesign: Boolean = true
}
